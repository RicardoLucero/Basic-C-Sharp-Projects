using System;
using System.Collections.Generic;
using System.Text;

namespace VoidMethodAssignment
{
    class Numbers
    {
        public static void MyMethod(int firstNum, int secondNum)//the method wants two integers as the arguments.
        {
            int total = 356 / firstNum;// we take the first parameter and run a math equation to it.
            Console.WriteLine(secondNum);// the second parameter we just print to the screen.
        }
    }
}
